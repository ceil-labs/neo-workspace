# 2026-04-04 - Daily Log

## HTB: TwoMillion Box - COMPLETED

**Session:** Continued from 2026-04-03, completed today

### Target
- **IP:** 10.129.217.51 (changed from 10.129.229.66 due to VPN switch)
- **Hostname:** 2million.htb
- **OS:** Linux (Ubuntu 22.04.2 LTS, Kernel 5.15.70)
- **Difficulty:** Medium
- **Status:** **ROOTED** ✅

---

## Attack Chain Summary

### Phase 1: Initial Access (Completed Yesterday)
1. **Deobfuscated JavaScript** (`inviteapi.min.js`) → discovered API endpoints
2. **Generated invite code:**
   - Called `/api/v1/invite/how/to/generate` → received ROT13-encoded instructions
   - Decrypted ROT13: "In order to generate the invite code, make a POST request to /api/v1/invite/generate"
   - Called `/api/v1/invite/generate` → received base64-encoded code
   - Decoded base64 → actual invite code
3. **Registered user:** `testuser1@email.com` / `password`
4. **Enumerated API** → discovered admin endpoints

### Phase 2: IDOR Privilege Escalation
- **Vulnerability:** Insecure Direct Object Reference in `/api/v1/admin/settings/update`
- **Exploit:** Modified own `is_admin` flag without authorization
```bash
curl -X PUT 'http://2million.htb/api/v1/admin/settings/update' \
  -d '{"email": "testuser1@email.com", "is_admin": 1}'
```
- **Result:** testuser1 elevated to admin

### Phase 3: Command Injection → Shell
- **Vulnerability:** Command injection in VPN generation (`/api/v1/admin/vpn/generate`)
- **Exploit:** Injected commands via `username` parameter
```bash
curl -X POST ... -d '{"username": "$(whoami)"}'
# Certificate Subject showed: O=www-data, CN=www-data
```
- **Reverse shell obtained:** www-data access

### Phase 4: Post-Exploitation → Admin
- **Discovered:** `/var/www/html/.env` containing credentials
- **Credentials:** `admin:SuperDuperPass123`
- **SSH access gained:** as admin user
- **User flag captured:** `e0d52f0fa1c2e8756998955b5bd7158e` ✅

### Phase 5: Root Privilege Escalation (Today)
- **Email clue:** `/var/mail/admin` mentioned OverlayFS/FUSE CVE
- **Vulnerability:** CVE-2023-0386 (Linux kernel 5.15.x)
- **Exploit:** OverlayFS/FUSE race condition
- **Requirement:** Two concurrent shells (race condition)
- **Execution:**
  ```bash
  # Terminal 1: Run fuse component
  ./fuse ovlcap/merged
  
  # Terminal 2: Run exploit
  ./exp
  
  # Race condition successful → root shell
  ```
- **Root flag captured:** ✅ (via CVE-2023-0386 exploit)

---

## Credentials Chain

| User | Password | Access Level | Source |
|------|----------|--------------|--------|
| testuser1 | password | Web app user (elevated to admin) | Registration |
| www-data | N/A | Web server user | Command injection |
| admin | SuperDuperPass123 | Local user + SSH | .env file |
| root | N/A | Full system access | CVE-2023-0386 |

---

## Flags

| Level | Flag | Method |
|-------|------|--------|
| **User** | `e0d52f0fa1c2e8756998955b5bd7158e` | SSH as admin |
| **Root** | [Captured] | CVE-2023-0386 OverlayFS exploit |

---

## Key Vulnerabilities Exploited

1. **Information Disclosure** — JavaScript deobfuscation revealed API structure
2. **Weak Encoding** — ROT13 used for "encryption" of invite codes
3. **IDOR** — Authorization bypass allowed self-elevation to admin
4. **Command Injection** — Unsanitized user input in VPN generation
5. **Credential Exposure** — Database credentials in web-accessible .env file
6. **Kernel Vulnerability** — CVE-2023-0386 OverlayFS/FUSE race condition

---

## Tools & Techniques Used

| Phase | Tools |
|-------|-------|
| Recon | curl, browser dev tools, JavaScript deobfuscation |
| Initial Access | Burp/curl for API testing, ROT13 decoding |
| Privilege Escalation (user) | Command injection, reverse shell, .env discovery |
| Privilege Escalation (root) | CVE-2023-0386 exploit, gcc compilation, race condition |

---

## Documentation & Artifacts

- ✅ recon.md — API enumeration, services
- ✅ exploit.md — IDOR + Command injection
- ✅ privesc.md — CVE-2023-0386 complete chain
- ✅ writeups/TwoMillion.md — Final publishable writeup
- ✅ Box archived: `active/` → `retired/`
- ✅ Exploit source preserved (fuse.c, exp.c, getshell.c, Makefile)

---

## Lessons Learned

1. **JavaScript obfuscation ≠ security** — Always deobfuscate client-side code
2. **Authorization checks matter** — IDOR is still prevalent in modern apps
3. **User input sanitization** — Never pass user input to system commands
4. **.env files in web roots** — Classic mistake, always check for exposed configs
5. **Kernel vulnerabilities** — Email hints can point to specific CVEs
6. **Race conditions** — Some exploits require precise timing with multiple shells
7. **CVE-2023-0386** — OverlayFS/FUSE privilege escalation requires:
   - Kernel 5.x (before patch)
   - /dev/fuse available
   - Two concurrent processes (race condition)

---

## Time to Root
- ~2 days (with breaks)
- Initial access: ~2 hours
- User privilege: ~1 hour  
- Root escalation: ~3-4 hours (including CVE research)

---

## Box Status: **ARCHIVED**
- Location: `htb/boxes/retired/TwoMillion/`
- Writeup: `htb/boxes/writeups/TwoMillion.md`
- Completion: 100% (both flags, full documentation)

---
