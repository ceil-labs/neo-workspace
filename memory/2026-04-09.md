# 2026-04-09 — HTB: Administrator

## Session Summary
Scaffolded and progressed through HTB box **Administrator** (10.129.245.95).

## Progress Log

### Recon
- Target: Windows Server 2022 Domain Controller (hostname: DC, domain: `administrator.htb`)
- Open ports: 21 (FTP), 53 (DNS), 88 (Kerberos), 135 (RPC), 139 (NetBIOS), 389/3268 (LDAP), 445 (SMB), 593 (RPC over HTTP), 5985 (WinRM)
- Initial credentials provided: `Olivia` / `ichliebedich`

### Enumeration
- Ran `bloodhound-python` and loaded data into BloodHound
- Key BloodHound findings (Guided Questions):
  - Q2: Olivia has **GenericAll** on Michael
  - Q3: Michael has **ForceChangePassword** on Benjamin
  - Q4: Benjamin is in **Share Moderators**

### Exploitation / Lateral Movement
- **WinRM access as Olivia** confirmed — Olivia is in Remote Management Users
- **Laterally moved to Michael** via WinRM after resetting Michael's password with Olivia's GenericAll
- **Changed Benjamin's password** via `rpcclient` + `setuserinfo2` from Michael's session
- **Accessed FTP as Benjamin** and downloaded `Backup.psafe3`
- **Cracked Backup.psafe3** with hashcat mode 5200 → master password: `tekieromucho`
- **Accessed Emily's account** using credentials from Backup.psafe3 and confirmed WinRM login

### Loot
- `Backup.psafe3` stored in box loot folder
- Master password: `tekieromucho`

## Progress Update (Late Session)
- Confirmed Emily has **GenericWrite** on Ethan (Q8)
- Added fake SPN to ethan via `setspn` from Emily's WinRM session
- Encountered `impacket-GetUserSPNs` CCache issue — TGT reuse fails on some impacket versions
- Spawned subagent to research targeted kerberoasting extraction alternatives
- **Next attack vector**: Use `impacket-getST`, direct password auth with `GetUserSPNs`, or Rubeus from WinRM

## Progress Update (2026-04-11 Session)
- Target IP reset by HTB to `10.129.213.179`
- Re-added fake SPN to Ethan via Emily's WinRM session
- Successfully extracted TGS hash using `impacket-GetUserSPNs`
- **Cracked Ethan's password** with hashcat `-m 13100` → `limpbizkit`
- Updated box documentation via `htb_document_progress` skill

## Next Steps
- Verify Ethan's privileges (Domain Admin? Delegation?)
- Capture root/admin flag
- Continue with remaining HTB guided questions
- Finalize documentation and retire box

## Notes
- Password Safe v3 cracking works with hashcat `-m 5200`
- `pwsafe` package on Kali requires GTK+ (GUI); for CLI-only VPS, consider Python parser or manual extraction
