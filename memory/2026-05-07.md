# Daily Log — 2026-05-07

## /new Greeting Delivery Bug — Confirmed

Victor tested `/new` with Neo after creating `BOOTSTRAP.md`.

**Result:** Same as Ceil — system ack ✅, but no greeting delivered ❌. Requires follow-up message.

**Pattern:** Both Ceil and Neo affected. The BOOTSTRAP.md is being read (session startup executes), but the generated greeting doesn't route through Telegram's deliver pipeline after `/new`.

**Open issue:** GitHub #68955

---

## Session Start

**Context:** Continuing from previous sessions. Neo workspace now has BOOTSTRAP.md.

