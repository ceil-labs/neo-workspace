# Neo — Daily Memory — 2026-04-12

## HTB: Footprinting Lab - Medium ✅
- **Target:** `10.129.202.41` (WINMEDIUM, Windows 10/Server 2019)
- **Goal:** Obtain HTB user credentials
- **Completed:** Yes — HTB creds found

### Attack Path
1. `showmount -e 10.129.202.41` → NFS `/TechSupport` world-readable
2. Mount NFS → 138 tickets → only `ticket4238791283782.txt` non-empty → found `alex.g@web.dev.inlanefreight.htb:lol123!mD`
3. Credential test: `alex` (SAM name, not UPN `alex.g`) works on **RDP**
4. RDP in → SMB shares → `devshare/important.txt` → `sa:87N1ns@slls83`
5. SSMS as `sa` (sysadmin) → query `accounts.dbo.devsacc` → `HTB:lnch7ehrdn437AoqVPK4zWR`

### Key Lesson
Username format matters on Windows — UPN `alex.g@...` only worked as short SAM name `alex`.

### HTB Creds
- `alex:lol123!mD` (intermediate)
- `sa:87N1ns@slls83` (intermediate)
- `HTB:lnch7ehrdn437AoqVPK4zWR` ← **final target**

---

## HTB: Footprinting Lab - Hard 🚧
- **Target:** `10.129.202.20` (was `10.129.61.168` before lab restart)
- **Hostname:** `NIXHARD` (Linux, Dovecot mail server)
- **Goal:** Obtain HTB user credentials
- **Status:** Enumeration started; nmap scan done

### Open Ports
- 22/tcp SSH (OpenSSH 8.2p1 Ubuntu)
- 110/tcp POP3 (Dovecot, STARTTLS)
- 143/tcp IMAP (Dovecot, STARTTLS)
- 993/tcp SSL/IMAP
- 995/tcp SSL/POP3

### Notes
- Mail server is MX + backup server for internal domain accounts → high probability HTB user mailbox or credentials in mail data
- Enumeration path: IMAP/POP3 user enumeration → credential testing → SSH credential reuse
- Port 25 (SMTP) not open — mail likely internal-only

---

## Infrastructure: n8n Redirect Fix (applied by Neo)
- **File modified:** `/home/openclaw/.openclaw/projects/agent-platform/nginx-proxy.conf`
- **Change:** Added `absolute_redirect off;` to server block listening on port 3000
- **Effect:** Changed `Location: http://host:3000/n8n/` to `Location: /n8n/` (relative redirect, no port/scheme leak via Tailscale HTTPS)
- **Container restarted:** `agent-platform-proxy-1` via `docker compose restart proxy`
- **Confirmed working:** `https://srv1405873.tailcd23a1.ts.net:8444/n8n` loads without secure cookie errors
- **Pending Ceil tasks:** Review nginx change, implement Rails auth gate for `/n8n/`, commit nginx-proxy.conf to repo

---

## Misc
- SSH key (`id_rsa`) was accidentally committed to workspace repo → purged from git history using `git filter-branch`, `.gitignore` updated with SSH key rules and HTB `loot/`/`files/` rules
