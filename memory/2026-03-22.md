# Session Log — 2026-03-22

## OpenClaw `isolatedSession` Discovery

**Finding:** `isolatedSession` for heartbeat is NOT available in v2026.3.13

### Timeline
| Event | Time (March 14, 2026) |
|-------|----------------------|
| v2026.3.13 tagged | 18:03 UTC |
| `isolatedSession` commit merged | 23:28 UTC (5.5 hours later) |

### What Happened
- Added `isolatedSession: true` to Neo's heartbeat config
- Gateway aborted with: `agents.list.1.heartbeat: Unrecognized key: "isolatedSession"`
- Docs show it as available (built from `main` branch)
- CHANGELOG.md doesn't mention it (never added to release notes)

### Root Cause
The feature was committed to `main` after v2026.3.13 was tagged. The running gateway binary doesn't have the schema field yet.

### Working Config (v2026.3.13)
```json
"heartbeat": {
  "every": "60m",
  "model": "opencode-go/minimax-m2.5",
  "target": "last",
  "lightContext": true,
  "ackMaxChars": 300
}
```

### Token Efficiency Note
- `lightContext: true` still saves tokens by only loading `HEARTBEAT.md`
- `isolatedSession` would further reduce from ~100K to ~2-5K tokens per run
- Full isolation available once v2026.3.14 releases

### Skills Created Today
1. **htb_document_progress** — Updates box docs from session activity
   - Location: `~/.openclaw/workspace-neo/skills/htb_document_progress/`
   - Usage: `htb_document_progress --box <name>`

2. **HEARTBEAT.md configured** — Documentation hygiene reminders
   - Checks active HTB box every 60 minutes
   - Notifies when session activity + stale docs detected
   - Silent when documentation current

---

*Documentation hygiene is critical for pentesting. Capture both failures and successes.*
